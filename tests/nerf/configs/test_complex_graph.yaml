defaults:
  - test_basic_graph
  - _self_
  
network:
  mlp_1:
    inputs: ['encoder_0']
  mlp_2:
    class_name: MLP
    inputs: ['encoder_0', 'mlp_0']
    meta_data:
      in_dim: 4
      out_dim: 5
      num_layers: 6
      layer_width: 7
  mlp_3:
    class_name: MLP
    inputs: ['mlp_0']
    meta_data:
      in_dim: 4
      out_dim: 5
      num_layers: 6
      layer_width: 7